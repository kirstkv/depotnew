<% if line_item == @current_item %>
<tr id="current_item">
<% else %>
<tr>
<% end %>
  <tr>
    <td><%= image_tag(line_item.product.image_url, :width=>"35") %></td>
    <td><%= line_item.quantity %>*<%= line_item.product.title %></td>
    <td></td>
    <td class="item_price"><%= number_to_currency(line_item.total_price, :unit=>"&pound;") %></td>
  </tr>